---
"@onderwijsin/nuxt-directus": minor
---

Implement the Directus REST proxy, request-scoped server client, browser proxy client, and typed
`useDirectus`/`useDirectusServer` command helpers with server-only credential selection.
